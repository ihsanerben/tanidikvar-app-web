"use client";
import { useState,type FormEvent } from "react";import { apiRequest } from "@/lib/client-api";
type Mode="verify"|"resend"|"forgot"|"reset";
const config={verify:{path:"/auth/verify-email",title:"E-postanı doğrula",button:"Doğrula"},resend:{path:"/auth/resend-verification",title:"Doğrulama e-postasını yeniden gönder",button:"Gönder"},forgot:{path:"/auth/forgot-password",title:"Şifreni yenile",button:"Yenileme bağlantısı gönder"},reset:{path:"/auth/reset-password",title:"Yeni parola belirle",button:"Parolayı değiştir"}};
export function AuthActionForm({mode,initialToken=""}:{mode:Mode;initialToken?:string}){const [message,setMessage]=useState(""),[busy,setBusy]=useState(false);const item=config[mode];async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const data=new FormData(event.currentTarget);const body=mode==="verify"?{token:data.get("token")} : mode==="reset"?{token:data.get("token"),password:data.get("password")}:{email:data.get("email")};try{await apiRequest(item.path,{method:"POST",body:JSON.stringify(body)});setMessage(mode==="forgot"||mode==="resend"?"E-posta adresi uygunsa gönderim başlatıldı.":"İşlem tamamlandı.");}catch(reason){setMessage(reason instanceof Error?reason.message:"İşlem tamamlanamadı.");}finally{setBusy(false);}}return <form className="stack-form" onSubmit={submit}><h1>{item.title}</h1>{mode==="forgot"&&<p className="auth-recovery-description">Hesabına bağlı e-posta adresini yaz. Şifreni güvenle yenileyebilmen için sana bir bağlantı göndereceğiz.</p>}{mode==="reset"&&<p className="auth-recovery-description">E-postana gönderilen kodu kullan ve hesabın için yeni, güçlü bir şifre belirle.</p>}{mode==="resend"||mode==="forgot"?<label>E-posta<input name="email" type="email" required autoComplete="email" placeholder="ornek@eposta.com"/></label>:<label>Tek kullanımlık kod<input name="token" required defaultValue={initialToken} autoComplete="one-time-code"/></label>}{mode==="reset"&&<label>Yeni şifre<input name="password" type="password" required minLength={12} maxLength={72} autoComplete="new-password" placeholder="En az 12 karakter"/></label>}{message&&<p role="status" className="muted">{message}</p>}<button className="button" disabled={busy}>{busy?"İşleniyor…":item.button}</button></form>}
