"use client";import {useRouter} from "next/navigation";import {useState,type FormEvent} from "react";import {apiRequest} from "@/lib/client-api";export function PollCreateForm({universityId,programId}:{universityId:string;programId?:string}){const router=useRouter(),[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget,data=new FormData(form),options=[data.get("first"),data.get("second"),data.get("third")].filter(value=>typeof value==="string"&&value.trim());try{await apiRequest("/polls",{method:"POST",body:JSON.stringify({universityId,programId:programId??null,question:data.get("question"),options,verifiedOnly:data.get("verifiedOnly")==="on",closesAt:null})});form.reset();setMessage("Anket yayınlandı.");router.refresh();}catch(error){setMessage(error instanceof Error?error.message:"Anket yayınlanamadı.");}}return <form className="stack-form decision-form" onSubmit={submit}><h3>Tanıdık anketi aç</h3><label>Soru<input name="question" minLength={10} maxLength={300} required/></label><label>Birinci seçenek<input name="first" maxLength={120} required/></label><label>İkinci seçenek<input name="second" maxLength={120} required/></label><label>Üçüncü seçenek (isteğe bağlı)<input name="third" maxLength={120}/></label><label><input type="checkbox" name="verifiedOnly"/> Yalnız doğrulanmış öğrenci ve mezunlar oy versin</label><button className="button">Anketi yayınla</button>{message&&<p role="status">{message}</p>}</form>}
