"use client";
import {useState,type FormEvent} from "react";
import {apiRequest} from "@/lib/client-api";
export function ContactForm(){const[message,setMessage]=useState(""),[busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const form=event.currentTarget,data=new FormData(form);try{await apiRequest("/contact",{method:"POST",body:JSON.stringify({name:data.get("name"),email:data.get("email"),subject:data.get("subject"),message:data.get("message")})});form.reset();setMessage("Mesajın gönderildi. En kısa sürede sana döneceğiz.");}catch(error){setMessage(error instanceof Error?error.message:"Mesaj gönderilemedi.");}finally{setBusy(false);}}return <form className="contact-form" onSubmit={submit}><div><label>Adın<input name="name" maxLength={120} required/></label><label>E-posta adresin<input name="email" type="email" maxLength={254} required/></label></div><label>Konu<input name="subject" maxLength={160} required/></label><label>Mesajın<textarea name="message" minLength={10} maxLength={5000} rows={6} required/></label><button className="button" disabled={busy}>{busy?"Gönderiliyor…":"Mesajı gönder"}</button>{message&&<p role="status">{message}</p>}</form>}
