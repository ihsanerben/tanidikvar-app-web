import Link from "next/link";import {getQuestions} from "@/lib/api/questions";import {getTanidiklar} from "@/lib/api/profiles";import {questionSegment} from "@/lib/public-url";
export async function ContextCommunity({universityId,departmentId,view="all"}:{universityId:string;departmentId?:string;view?:"all"|"questions"|"people"}){
 const [questions,people]=await Promise.all([view==="people"?null:getQuestions("",0,{scope:departmentId?"UNIVERSITY_DEPARTMENT":"UNIVERSITY",universityId,departmentId,sort:"MOST_COMMENTED"}).catch(()=>null),view==="questions"?null:getTanidiklar("",0,{universityId,departmentId}).catch(()=>null)]),query=new URLSearchParams({universityId,...(departmentId?{departmentId}:{})});
 return <>{(view==="all"||view==="questions")&&<section id="sorular" className="content-section university-tab-panel"><div className="section-heading"><div><h2>Sorular</h2><p>Bu üniversite hakkında topluluğa yöneltilen güncel sorular.</p></div><Link href={`/sorular?${query}`}>Tümünü gör</Link></div>{questions?.items.length?<ul className="question-list">{questions.items.slice(0,12).map(question=><li key={question.id}><h3><Link href={`/soru/${questionSegment(question.title,question.id)}`}>{question.title}</Link></h3>{question.body&&<p>{question.body}</p>}<span>{question.statistics.totalAnswerCount} yorum · {question.statistics.viewCount} görüntülenme</span></li>)}</ul>:<div className="empty-state"><h3>Henüz soru yok</h3><p>Bu üniversite hakkındaki ilk soruyu topluluğa yöneltebilirsin.</p><Link className="button" href={`/soru-sor?${query}`}>İlk soruyu sor</Link></div>}</section>}
 {(view==="all"||view==="people")&&<section id="tanidiklar" className="content-section university-tab-panel"><div className="section-heading"><div><h2>Tanıdıklar</h2><p>Bu üniversiteyle doğrulanmış bağı bulunan öğrenciler ve mezunlar.</p></div><Link href={`/tanidiklar?${query}`}>Tümünü gör</Link></div>{people?.items.length?<ul className="catalog-grid university-people-grid">{people.items.slice(0,12).map(person=><li key={person.id}><h3><Link href={`/tanidik/${person.id}`}>{person.name}</Link></h3><p>{[person.departmentName,person.classYear?`${person.classYear}. sınıf`:person.graduationYear?`${person.graduationYear} mezunu`:null].filter(Boolean).join(" · ")}</p><span>{person.tanidikAnswerCount} Tanıdık yorumu · {person.communityAnswerCount} topluluk yorumu</span></li>)}</ul>:<div className="empty-state"><h3>Henüz Tanıdık yok</h3><p>Bu üniversite için doğrulanan öğrenciler ve mezunlar burada görünecek.</p><Link className="button secondary" href="/hesabim/tanidik-basvurusu">Tanıdık ol</Link></div>}</section>}</>;
}
