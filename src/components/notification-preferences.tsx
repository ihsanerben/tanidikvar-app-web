"use client";import{useEffect,useState,type FormEvent}from"react";import{apiRequest}from"@/lib/client-api";type Preferences={inAppEnabled:boolean;emailEnabled:boolean;emailFrequency:string;questionRoutingEnabled:boolean;version:number};export function NotificationPreferences(){const[prefs,setPrefs]=useState<Preferences|null>(null),[message,setMessage]=useState("");useEffect(()=>{apiRequest<Preferences>("/me/notification-preferences").then(setPrefs);},[]);if(!prefs)return <p className="muted">Bildirim tercihleri yükleniyor…</p>;async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const data=new FormData(event.currentTarget);try{const value=await apiRequest<Preferences>("/me/notification-preferences",{method:"PUT",body:JSON.stringify({inAppEnabled:data.has("inAppEnabled"),emailEnabled:data.has("emailEnabled"),questionRoutingEnabled:data.has("questionRoutingEnabled"),emailFrequency:data.get("emailFrequency"),version:prefs!.version})});setPrefs(value);setMessage("Bildirim tercihleri güncellendi.");}catch(reason){setMessage(reason instanceof Error?reason.message:"Tercihler kaydedilemedi.");}}return <form className="stack-form" onSubmit={submit}><h2>Bildirim tercihleri</h2><label className="check-label"><input name="inAppEnabled" type="checkbox" defaultChecked={prefs.inAppEnabled}/> Uygulama içi bildirimler</label><label className="check-label"><input name="questionRoutingEnabled" type="checkbox" defaultChecked={prefs.questionRoutingEnabled}/> Deneyimime uygun sorular</label><label className="check-label"><input name="emailEnabled" type="checkbox" defaultChecked={prefs.emailEnabled}/> İzinli e-posta bildirimleri</label><label>E-posta sıklığı<select name="emailFrequency" defaultValue={prefs.emailFrequency}><option value="IMMEDIATE">Anında</option><option value="DAILY">Günlük özet</option><option value="WEEKLY">Haftalık özet</option><option value="NEVER">Asla</option></select></label>{message&&<p role="status" className="muted">{message}</p>}<button className="button">Tercihleri kaydet</button></form>}
