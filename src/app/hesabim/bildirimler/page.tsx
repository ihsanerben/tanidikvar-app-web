import Link from "next/link";import {redirect} from "next/navigation";import {currentUser} from "@/lib/session";import {AccountDashboard} from "@/components/account-dashboard";import {NotificationPreferences} from "@/components/notification-preferences";
export const metadata={title:"Bildirimler",robots:{index:false,follow:false}};export default async function Page(){if(!await currentUser())redirect("/giris");return <section className="legacy-account-page"><Link href="/hesabim">← Hesabıma dön</Link><h1>Bildirimler</h1><div className="legacy-account-details"><AccountDashboard mode="notifications"/><NotificationPreferences/></div></section>}
